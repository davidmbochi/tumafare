spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tumafare
    username: tumafare
    password: tumafare
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

application:
  mailing:
    frontend:
      activation-url: https://tumafare.com/auth/activate

  password:
    change:
      change-password-url: https://tumafare.com/password/change